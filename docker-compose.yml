version: '3.8'

services:
  koresync:
    build: .
    container_name: koresync
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
